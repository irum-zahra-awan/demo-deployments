# Copy this file to secrets.toml (same folder) and put your real key in it.
# secrets.toml is in .gitignore — it will never be pushed to GitHub.
# On Streamlit Community Cloud, paste this same line into
# App settings -> Secrets, instead of using this file.

GEMINI_API_KEY = "paste-your-key-here"
